class Whatsapp_OTP():
        def post(self):

        if not current_user.akerun_user_id:
            user_auth = default

            r = requests.post(
                akerun_url,
                headers,
                akerun_user_id,
                email,
                user_auth
            )
            return r(), 200
        else:
            pass


class Whatsapp_():
    def post(self):


        r = request.post(
                    headers, 
                    akerun_user_id,
                    schedule,
                    user_id.booking[start_datetime],
                    user_id.booking[end_datetime],
                    role
                    )
        
        return r(), 200